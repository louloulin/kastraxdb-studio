# MagicDB 重命名为 MagicDB 的完整指南

本文档提供了将 MagicDB 项目重命名为 MagicDB 的详细说明，包括所有必要的步骤和脚本。

## 概述

重命名过程包括以下几个方面：

1. 将文件内容中所有 "magicdb" 替换为 "magicdb"
2. 更新 Java/Kotlin 包声明和导入语句
3. 重命名文件和目录
4. 迁移数据库配置和数据
5. 验证重命名是否成功

## 提供的脚本

我们创建了几个脚本来帮助完成重命名过程：

1. **master_rename.sh** - 主脚本，协调整个重命名过程
2. **database_migration.sh** - 专门处理数据库迁移的脚本
3. **rename_magicdb_to_magicdb_complete.sh** - 核心重命名过程的独立脚本
4. **fix_java_packages.sh** - 修复 Java/Kotlin 包声明的脚本
5. **verify_rename.sh** - 验证所有 "magicdb" 实例是否已重命名的脚本

## 前提条件

- macOS 操作系统（脚本使用 macOS 特定命令）
- Bash shell
- 标准 Unix 工具：find, sed, grep
- Git（可选，用于版本控制）

## 步骤说明

### 1. 准备工作

在开始重命名过程之前，建议：

- 创建项目备份
- 将任何待处理的更改提交到版本控制系统
- 关闭可能正在访问项目文件的任何 IDE 或编辑器

### 2. 运行主脚本

主脚本处理整个重命名过程：

```bash
./master_rename.sh
```

该脚本将：
- 执行预检查以确保环境准备就绪
- 创建项目备份
- 执行重命名过程
- 验证结果
- 提供重命名后的步骤指导

### 3. 数据库迁移

运行主脚本后，运行数据库迁移脚本：

```bash
./database_migration.sh
```

该脚本将：
- 识别数据库路径和配置
- 创建数据库备份
- 更新数据库路径和配置
- 将数据从 ~/.magicdb 迁移到 ~/.magicdb

### 4. 重命名后的步骤

重命名过程完成后：

1. **清理并重新构建项目**：
   - 对于 Maven：`mvn clean install -DskipTests`
   - 对于客户端：`cd magicdb-client && yarn install && yarn build`

2. **更新 IDE 配置**：
   - 在 IDE 中刷新/重新加载项目
   - 更新任何运行配置

3. **测试应用程序**：
   - 运行单元测试
   - 启动应用程序并验证基本功能

4. **检查任何剩余问题**：
   - 查找与重命名相关的任何运行时错误
   - 检查日志中是否有任何遗漏的 'magicdb' 引用

## 故障排除

### 如果主脚本失败：

1. 检查日志文件（rename_log_*.txt）了解详情
2. 手动修复任何问题
3. 运行验证脚本检查所有实例是否已重命名：
   ```bash
   ./verify_rename.sh
   ```

### 如果需要修复 Java/Kotlin 包：

运行包修复脚本：
```bash
./fix_java_packages.sh
```

### 如果遇到数据库问题：

1. 检查数据库迁移日志（db_migration_log_*.txt）
2. 从数据库迁移脚本创建的备份中恢复
3. 按照日志文件中提供的指导操作

## 手动验证

即使在运行脚本后，也建议手动验证：

1. **项目结构**：确保所有目录和文件已正确重命名
2. **代码编译**：确保项目编译没有错误
3. **应用程序启动**：验证应用程序能否正确启动
4. **基本功能**：测试基本功能以确保它们按预期工作
5. **数据库访问**：确认应用程序能否正确访问数据库

## 恢复原始版本

如果需要恢复到原始的 MagicDB 项目：

1. 从主脚本创建的备份中恢复
2. 如果已迁移数据库，从数据库迁移脚本创建的备份中恢复

## 支持

如果在重命名过程中遇到任何问题，请：

1. 检查日志文件了解详情
2. 查看本文档获取指导
3. 联系项目维护者寻求帮助

---

**注意**：这些脚本按原样提供，应谨慎使用。在进行任何重命名过程之前，请确保您有备份。
