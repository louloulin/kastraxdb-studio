# MagicDB 重命名为 MagicDB 的说明

## 概述

我们提供了一系列脚本，用于将整个 MagicDB 项目重命名为 MagicDB。这些脚本会处理代码、配置文件、目录结构和数据库路径等所有方面的重命名工作。

## 提供的脚本

1. **advanced_rename.sh** - 高级重命名脚本，处理复杂边缘情况并提供详细报告
2. **master_rename.sh** - 主脚本，协调整个重命名过程
3. **database_migration.sh** - 专门处理数据库迁移的脚本
4. **fix_java_packages.sh** - 修复 Java/Kotlin 包声明的脚本
5. **verify_rename.sh** - 验证所有 "magicdb" 实例是否已重命名的脚本

## 使用方法

1. **运行高级重命名脚本**:
   ```bash
   ./advanced_rename.sh
   ```
   
   该脚本会:
   - 创建项目备份
   - 执行全面的重命名过程
   - 处理特殊情况
   - 验证结果
   - 生成详细报告
   - 提供后续步骤指导

2. **数据库迁移**:
   ```bash
   ./database_migration.sh
   ```
   
   该脚本会处理数据库相关的迁移工作。

## 重命名后的步骤

1. **清理并重新构建项目**:
   - Maven: `mvn clean install -DskipTests`
   - 客户端: `cd magicdb-client && yarn install && yarn build`

2. **更新 IDE 配置**:
   - 在 IDE 中刷新/重新加载项目
   - 更新运行配置

3. **测试应用程序**:
   - 运行单元测试
   - 验证基本功能

## 注意事项

- 脚本会自动创建备份，但建议您在运行脚本前手动备份项目
- 脚本设计用于 macOS 系统，使用 macOS 版本的 sed 命令
- 如果遇到问题，请查看生成的日志和报告文件
- 某些 IDE 特定的配置文件可能需要手动更新

## 支持

如果在重命名过程中遇到任何问题，请查看日志文件和报告文件，或联系项目维护者寻求帮助。
